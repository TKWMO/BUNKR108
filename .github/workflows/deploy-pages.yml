name: Deploy to GitHub Pages (disabled)

# This repository already deploys via `.github/workflows/static.yml`.
# This older workflow used deprecated Pages actions and commonly fails.
# Keep it available for manual runs only (Actions tab) to avoid noisy failures on push.

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is intentionally disabled; use 'Deploy static content to Pages'."
